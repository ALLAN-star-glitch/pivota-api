# access-management

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build access-management` to build the library.

## Running unit tests

Run `nx test access-management` to execute the unit tests via [Jest](https://jestjs.io).
